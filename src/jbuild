(jbuild_version 1)

(executable
  ((name main)
   (libraries (core async async_extra))
   (preprocess (pps (ppx_jane)))
   (flags (-w -40 -g))
   (modes (native))
  ))

(install
 ((section bin)
  (files ((main.exe as main)))))

