(library
  (inline_tests)
  (name pickles_types)
  (public_name pickles_types)
  (flags -warn-error -27)
  (preprocess (pps ppx_coda ppx_jane ppx_deriving.std))
  (libraries
    snarky
    core_kernel
    module_version))
