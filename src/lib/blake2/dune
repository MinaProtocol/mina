(library
  (name blake2)
  (public_name blake2)
  (preprocess (pps ppx_jane ppx_coda ppx_version ppx_deriving.eq ppx_deriving_yojson bisect_ppx -- --conditional))
  (inline_tests)
  (libraries
    core_kernel
    digestif
    ppx_version.runtime
  ))
