(library
  (name transition_handler)
  (public_name transition_handler)
  (inline_tests)
  (libraries core_kernel protocols coda_base genesis_ledger consensus transition_frontier ledger_builder)
  (preprocess (pps ppx_jane)))
