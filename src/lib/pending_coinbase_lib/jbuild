(jbuild_version 1)

(library
 ((name pending_coinbase_lib)
  (public_name pending_coinbase_lib)
  (flags (:standard -short-paths -warn-error -9-58))
  (library_flags (-linkall))
  (inline_tests)
  (libraries (core_kernel coda_base))
  (preprocess (pps (ppx_jane ppx_deriving.eq bisect_ppx -conditional)))
  (synopsis "Pending coinbase implementation")))

