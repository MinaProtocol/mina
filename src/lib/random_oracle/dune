(library
  (name random_oracle)
  (public_name random_oracle)
  (preprocess (pps ppx_jane ppx_coda ppx_deriving.eq ppx_deriving_yojson))
  (inline_tests)
  (libraries
    core_kernel
    snarky
    snarky_blake2
    random_oracle_base
    crypto_params))
