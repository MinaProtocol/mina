(library
  (name lite_curve_choice)
  (public_name lite_curve_choice)
  (preprocessor_deps ../../config.mlh)
  (preprocess (pps ppx_optcomp ppx_deriving.eq ppx_version))
  (libraries
    snarkette core_kernel))
