# Keep this config.toml and do not make one global with `../stubs/config.toml` as
# stubs must use the vendored libraries.
# plonk_wasm cannot be compiled, for now, with the vendored libraries as it is
# using nightly and the stdlib is required.
[profile.release.target.wasm32-unknown-unknown]
debug = true

[package.metadata.wasm-pack.profile.release]
wasm-opt = false
#wasm-opt = ["-O4", "--detect-features", "--enable-mutable-globals" ]
#wasm-opt = ["-O4", "--enable-mutable-globals"]