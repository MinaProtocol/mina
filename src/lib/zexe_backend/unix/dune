(library
  (inline_tests)
  (name zexe_backend_unix)
  (public_name zexe_backend.unix)
  (flags -warn-error -27)
  (instrumentation (backend bisect_ppx))
  (preprocess (pps ppx_version ppx_jane ppx_deriving_yojson ppx_deriving.std))
  (libraries
    zexe_backend
    key_cache.unix
    async
    core))

