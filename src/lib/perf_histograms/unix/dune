(library
 (name perf_histograms_unix)
 (public_name perf_histograms.unix)
 (libraries
   ;; opam libraries
   graphql-async
   yojson
   graphql
   integers
   fieldslib
   base
   core_kernel
   ;; local libraries
   perf_histograms
   graphql_basic_scalars
   graphql_wrapper
 )
 (preprocess (pps ppx_version))
 (instrumentation (backend bisect_ppx))
 (synopsis "Utils that depends on the unix library"))
