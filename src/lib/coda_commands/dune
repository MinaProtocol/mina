(library
  (name coda_commands)
  (public_name coda_commands)
  (libraries 
    ; opam deps
    core
    ; libs
    node_addrs_and_ports coda_base coda_lib coda_version)
  (preprocess (pps ppx_coda ppx_jane)))
