(jbuild_version 1)

(library
 ((name transaction_snark_scan_state)
  (public_name transaction_snark_scan_state)
  (flags (:standard -short-paths -warn-error -58))
  (inline_tests)
  (library_flags (-linkall))
  (libraries (pipe_lib digestif.ocaml core async async_extra sgn transaction_snark coda_base protocols logger))
  (preprocess (pps (ppx_jane ppx_deriving.eq)))
  (synopsis "Parallel scan over an infinite stream (incremental map-reduce)")))

