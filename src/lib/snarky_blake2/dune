(library
  (name snarky_blake2)
  (public_name snarky_blake2)
  (preprocess (pps ppx_version ppx_jane ppx_deriving.eq))
  (inline_tests)
  (libraries
    snarky
    blake2
    digestif
    core_kernel ))
