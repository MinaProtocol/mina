(library
  (name random_oracle_base)
  (public_name random_oracle_base)
  (preprocess (pps ppx_jane ppx_coda ppx_deriving.eq ppx_deriving_yojson))
  (inline_tests)
  (libraries
    core_kernel
    fold_lib
    tuple_lib
    blake2
    coda_digestif
    base58_check
    module_version
    ))
