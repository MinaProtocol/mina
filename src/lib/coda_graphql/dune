(library
  (name coda_graphql)
  (public_name coda_graphql)
  (libraries 
    ; opam deps
    async core cohttp graphql-async graphql-cohttp daemon_status
    ; libs
    auxiliary_database coda_base coda_commands coda_lib lite_compat)
  (preprocess (pps ppx_coda ppx_jane)))
