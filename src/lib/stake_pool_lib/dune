(library
  (name stake_pool_lib)
  (inline_tests)
  (public_name stake_pool_lib)
  (preprocess (pps ppx_jane ppx_deriving.eq))
  (libraries
    pipe_lib
    ledger_builder
    consensus
    protocols
    signature_lib
    core_kernel
    coda_base))
