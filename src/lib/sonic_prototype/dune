(library
  (name sonic_prototype)
  (modules backend default_backend laurent srs arithmetic_circuit constraints commitment_scheme utils test_laurent test_constraints test_commitment_scheme)
  (inline_tests)
  (preprocess (pps ppx_inline_test ppx_jane ppx_deriving.eq))
  (libraries core snarkette snarky snarky_taylor)
)
  
