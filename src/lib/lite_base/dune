(library
  (name lite_base)
  (public_name lite_base)
  (preprocess (pps ppx_jane ppx_deriving.eq))
  (libraries
    base
    coda_digestif
    hash_prefixes
    pedersen_lib
    fold_lib
    sparse_ledger_lib
    snarkette
    bin_prot))
