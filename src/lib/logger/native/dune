(library
 (name logger_native)
 (public_name logger.native)
 (library_flags -linkall)
 (inline_tests
  (flags -verbose -show-counts))
 (libraries
  ;; opam libraries
  result
  core
  core_kernel
  sexplib0
  bin_prot.shape
  base.caml
  base.base_internalhash_types
  ;; local libraries
  itn_logger
  interpolator_lib
  ppx_version.runtime
  mina_stdlib)
 (preprocess
  (pps ppx_compare ppx_deriving.std ppx_enumerate ppx_deriving_yojson ppx_let ppx_mina ppx_sexp_conv ppx_version))
 (instrumentation
  (backend bisect_ppx))
 (implements logger)
 (synopsis "Logging library"))
