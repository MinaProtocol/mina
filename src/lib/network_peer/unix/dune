(library
 (name network_peer_unix)
 (public_name network_peer.unix)
 (libraries
   ;; opam libraries
   graphql-async
   yojson
   graphql
   integers
   fieldslib
   base
   ;; local libraries
   graphql_basic_scalars
   network_peer
   graphql_wrapper
 )
 (preprocess (pps ppx_version))
 (instrumentation (backend bisect_ppx))
 (synopsis "Utils that depends on the unix library"))
