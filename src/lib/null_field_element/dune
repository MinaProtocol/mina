(library
  (name null_field_element)
  (public_name null_field_element)
  (flags :standard -short-paths -warn-error -58)
  (libraries snark_params)
  (preprocess
    (pps ppx_jane ppxlib.metaquot)))

(rule
  (targets null_field_element.ml)
  (deps
    (:< gen_values/gen_values.exe))
  (action
    (run %{<} %{targets})))