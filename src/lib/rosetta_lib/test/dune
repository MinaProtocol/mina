(executable
 (package rosetta_lib)
 (name test_encodings)
 (public_name test_encodings)
 (modes native)
 (modules test_encodings)
 (libraries
  ;; opam libraries
  alcotest
  ;; local libraries
  rosetta_coding
  rosetta_lib
  signature_lib)
 (instrumentation
  (backend bisect_ppx))
 (preprocess
  (pps ppx_version)))
