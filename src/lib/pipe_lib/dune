(library
  (name pipe_lib)
  (inline_tests)
  (public_name pipe_lib)
  (libraries core_kernel async o1trace logger)
  (preprocess (pps ppx_coda ppx_version ppx_jane ppx_deriving.make bisect_ppx -- --conditional)))
