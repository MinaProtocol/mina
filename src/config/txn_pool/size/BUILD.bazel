""" config/txn_pool/size

Configuration constant for transaction pool size.

Note this value needs to be consistent across nodes to prevent
spurious bans. See comment in transaction_pool.ml for more details.
"""

load("@bazel_skylib//rules:common_settings.bzl", "int_setting")

int_setting( name = "max", build_setting_default = 3000,
             visibility = ["//visibility:public"])

