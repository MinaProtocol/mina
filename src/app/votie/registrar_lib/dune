(library
  (name registrar_lib)
  (public_name votie.registrar_lib)
  (preprocess (pps ppx_jane ppx_coda ppx_deriving.eq ppx_deriving_yojson))
  (inline_tests)
  (libraries
    core_kernel
    votie_lib
    votie_rpcs
    commander
    universe ))

