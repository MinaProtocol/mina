(executable
  (package votie)
  (name votie)
  (public_name votie)
  (modes native)
  (libraries votie_lib snarky universe sponge core async)
  (preprocess (pps ppx_jane ppx_deriving.std))
  (modules votie)
)

(executable
  (package votie)
  (name voter)
  (public_name voter)
  (modes native)
  (libraries
    textutils.ascii_table
    votie_lib
    voter_lib
    commander
    votie_rpcs
    snarky
    universe
    core async)
  (preprocess (pps ppx_jane ppx_deriving.std ppx_deriving_yojson))
  (modules voter) )

(executable
  (package votie)
  (name voter_daemon)
  (public_name voter_daemon)
  (modes native)
  (libraries votie_lib voter_lib votie_rpcs snarky universe sponge core async async.persistent_connection)
  (preprocess (pps ppx_jane ppx_deriving.std ppx_deriving_yojson))
  (modules voter_daemon) )

(executable
  (package votie)
  (name registrar_daemon)
  (public_name registrar_daemon)
  (modes native)
  (libraries votie_lib registrar_lib votie_rpcs snarky universe sponge core async)
  (preprocess (pps ppx_jane ppx_deriving.std))
  (modules registrar_daemon))

(executable
  (package votie)
  (name registrar)
  (public_name registrar)
  (modes native)
  (libraries votie_lib registrar_lib votie_rpcs snarky universe sponge core async)
  (preprocess (pps ppx_jane ppx_deriving.std))
  (modules registrar))

