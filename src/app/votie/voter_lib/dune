(library
  (flags -w -2)
  (name voter_lib)
  (public_name votie.voter_lib)
  (preprocess (pps ppx_jane ppx_coda ppx_deriving.eq ppx_deriving_yojson))
  (inline_tests)
  (libraries
    core_kernel
    votie_lib
    votie_rpcs
    commander
    universe ))
