(jbuild_version 1)

(executable
  ((name coda)
  (public_name coda)
   (libraries
    ( base64
      core
      debug_assert
      client_lib
      linear_pipe
      coda_networking
      proposer
      gossip_net
      kademlia
      coda_lib
      coda_numbers
      protocols
      lite_lib
      lite_compat
      coda_base
      test_util
      ledger_builder
      ledger_builder_controller
      sparse_ledger_lib
      syncable_ledger
      blockchain_snark
      transaction_snark
      transaction_pool
      snarky
      snark_keys
      snark_pool
      precomputed_values
      network_pool
      snark_worker_lib
      keys_lib
      storage
      logger
      async
      async_extra
      rpc_parallel
      async_ssl
      cohttp
      cohttp-async
      parallel
      file_system
      yojson
      web_client_pipe
      work_selector ))
   (preprocessor_deps ("../../../config.mlh"))
   (preprocess (pps (ppx_jane ppx_deriving.eq ppx_deriving.make ppx_inline_test ppx_coda)))
   (flags (-short-paths -w -40 -g -warn-error +3+4+6+8+9+10+11+12+14+16+20+21+23+24+26+27+29+32..39+56-58+59+60+61))
   (modes (native))
  ))

