(executable
  (package rosetta)
  (name rosetta)
  (public_name rosetta)
  (modes native)
  (libraries core async ppx_version caqti caqti-async caqti-driver-postgresql cli_lib)
  (preprocess (pps ppx_coda ppx_version ppx_jane))
  )
