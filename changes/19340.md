Ship the post-hardfork mainnet runtime config. `genesis_ledgers/mainnet.json` now describes the network as it is after the 2026-09-03 hard fork: genesis timestamp `2026-09-03T18:00:00Z`, `proof.fork` at state hash `3NKHyxzgM9z4C1wUz5ZhJBmNhtDzDQGr1PGvWRwiCUcFvsdTco2T` (blockchain length 548146, global slot since genesis 958440), and the new genesis and epoch ledger hashes. The stop-slot schedule that armed that fork (`slot_tx_end`, `slot_chain_end` and `hard_fork_genesis_slot_delta` under `daemon`) is removed, because the fork it scheduled has happened; a daemon started with this config no longer stops at slot 393900. Node operators who run mainnet from the shipped config get the forked chain without further changes.

The Rosetta `docker-compose` example environment files are updated to match. Both `example.mainnet.env` and `example.devnet.env` now name the 4.0.0 images on Docker Hub instead of the retired 3.3.x images, which for devnet also moves the example off `gcr.io`. Both files now point `GUARDIAN_PRECOMPUTED_BLOCKS_URL` at the public bucket `https://storage.googleapis.com/mina_network_block_data`, which is the bucket the missing-blocks guardian can read for both networks.
