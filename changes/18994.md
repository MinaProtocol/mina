Fix chain_id mismatch at daemon startup: when computing the genesis state hash for chain_id, zero out both `total_stake` and `total_currency` from the genesis ledger instead of only `total_currency`. Previously, the missing `total_stake` zeroing caused the computed genesis state hash to diverge from the actual ledger hash, resulting in a chain_id mismatch that prevented the daemon from starting after a hard fork.
