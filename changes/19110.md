Add a benchmark for the PostgreSQL memory used by Mina's database helpers

Adds a small, self-contained benchmark that measures how many server-side prepared statements and how much backend memory a long-lived database connection accumulates through Mina's database query helpers. It reproduces and tracks the archive PostgreSQL backend memory growth reported in #18857 and runs in CI as a regression check. It does not change how a node runs.
