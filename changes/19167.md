Carry over pending source changes from the archive dry-run branch, so that the two trees do not drift apart. The syncable ledger states more clearly how it handles a depth mismatch during ledger sync. `Block_time` makes the timestamp conversion explicit and states the unit in its interface. The gossip receive path (`block_sink`, `pool_sink`, `mina_net2`, `signed_command_memo`) keeps the receive handling separate from the rest of the sink logic. No config, pipeline or packaging file is changed.
