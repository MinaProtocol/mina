A new `--hard-fork-output-file` for the replayer has been added, which will
induce the replayer to write a hard fork genesis config and ledgers based on its
final ledger state at the end of a replayer run. This is useful for archive
database validation during a hard fork.
