The daemon will now refuse to start if `MINA_HARDFORK_STATE_DIR` is set and not
equal to its actual config directory. This consistency check allows the hard
fork dispatcher in the auto mode packaging to assume that
`MINA_HARDFORK_STATE_DIR` is the correct location of the auto mode fork config.
