Generate random field elements in zkApp test transaction events and actions

The `update-state` subcommand of the zkApp test transaction tool filled the event and action arrays it generates with consecutive integers. It now fills them with random field elements, so that the generated arrays resemble real ones and do not collide with those of another transaction generated the same way. `--repeat-arrays` still emits identical arrays within a transaction.
