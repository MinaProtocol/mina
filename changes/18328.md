Fix a slow memory leak in the libp2p stream table in the mina daemon. This table
tracks the underlying streams for direct peer-to-peer RPC queries, so seed nodes
are expected to be particularly affected by this bug.
