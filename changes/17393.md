**Modified RPC interaction between snark workers and coordinator.** Coordinator now distributes work at proof level rather than transaction level, enabling greater parallelism in SNARK generation.

**Release note:** Snark coordinator and workers must be updated simultaneously to avoid communication errors.
