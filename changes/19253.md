Speed up block production for blocks with many transactions

Choosing which transactions and completed work to include in a block took time proportional to the square of the number of candidate transactions, so it grew steeply as blocks got larger. It is now proportional to the number of candidates. Block producers on networks with large transaction capacities should see block creation take noticeably less time; the blocks produced are unchanged.
