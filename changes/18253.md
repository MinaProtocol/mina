GraphQL queries on daemon node would no longer close the connection when there's errors encountered when resolving the query. Instead, the node would return error code 500, with the error message as string in the HTTP response body.
