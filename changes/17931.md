The new daemon config option `hard_fork_genesis_slot_delta` will track the
planned genesis slot of an upcoming hard fork in terms of the number of slots
between the `slot_chain_end` and the hard fork genesis slot. It is optional, and
currently absent by default.
