CI/Tooling: continue migrating bash `curl ... /graphql` calls to the typed `mina-graphql-client` CLI. Adds `fork-config` and `send-raw` subcommands, surfaces `inferredNonce` from the `account` subcommand, and migrates `scripts/hardfork/get_fork_config.sh` (kubectl-exec'd inside pods) and `scripts/mina-local-network/mina-local-network.sh` (sync poll, nonce queries, zkApp mutations). Removes the orphaned `scripts/mina-local-network/send-graphql-query.py` helper. Rosetta scripts are intentionally left alone — they will move to a dedicated rosetta client in a follow-up.
