Stop penalising peers for sending blocks the node already has

When a node received a copy of a block it already had or was already processing, it never reported the copy as handled. The copy timed out several minutes later and the sending peer was penalised, even though the block was valid. On well-connected nodes this affected a large share of incoming blocks. Such copies are now dropped immediately without penalising the sender.
