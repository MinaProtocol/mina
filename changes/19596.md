The archive schema indexes `user_commands.fee_payer_id`, `source_id` and `receiver_id`, which account lookups such as Rosetta `/search/transactions` filter on. Previously every such request scanned the whole table.
