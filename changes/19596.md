The archive schema indexes `user_commands.fee_payer_id`, `source_id` and `receiver_id`, and `upgrade.sql` adds them to existing archives. Account lookups such as Rosetta `/search/transactions` filter on those columns, and without the indexes such a search reads the whole table.
