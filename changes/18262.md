- Mina daemon now logs unhandled exceptions as structured JSON metadata (instead
  of crashing silently), including full exception details in the log output
- Both top-level CLI entrypoint exceptions and unhandled async exceptions during
  daemon startup are now caught and logged before the process exits
