Rosetta: include `nonce` (and `memo` when present) in zkapp-command transaction metadata returned by the `/block` endpoint. Previously `Zkapp_command_info.to_transaction` hard-coded `metadata = None`, so zk-app transactions were returned without a nonce even though the data was available.
