kind: Secret
apiVersion: v1
metadata:
  name: {{ .Values.dbPusher.script.secret.name }}
  labels:
    {{- include "ci-dashboard.labels" . | nindent 4 }}
data:
  {{- (.Files.Glob (printf "files/dbPusher/%s" .Values.dbPusher.script.name)).AsSecrets | nindent 2 }}
  {{- (.Files.Glob "files/dbPusher/requirements.txt").AsSecrets | nindent 2 }}