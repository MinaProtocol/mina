apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "itn-services.fullname" . }}-test-connection"
  labels:
    {{- include "itn-services.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "itn-services.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
