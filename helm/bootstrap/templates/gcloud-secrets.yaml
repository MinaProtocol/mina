{{- $root := . -}}
---
kind: Secret
apiVersion: v1
metadata:
  name: gcloud-keyfile
  labels:
    {{- include "bootstrap.labels" $root | nindent 4 }}
data:
  keyfile: |-
    {{ $root.Files.Get $root.Values.gcpKey.path | b64enc | indent 4 }}