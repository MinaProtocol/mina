(jbuild_version 1)

(executable
  ((name reformat)
   (public_name reformat)
   (libraries (core async))
   (preprocess (pps (ppx_jane)))
   (flags (-w -40 -g -warn-error -58))
   (modes (native))
  ))

