(** Stationary is a static site generator library. The basic idea is
    that you describe your site by building up a directory tree/filesystem,
    which is then built in a specified directory.

    Check out ??? for examples.
*)
module Html        = Html
module Attribute   = Attribute
module File        = File
module File_system = File_system
module Site        = Site
