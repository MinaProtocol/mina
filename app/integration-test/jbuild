(jbuild_version 1)

(executable
 ((name integration_test)
  (public_name integration_test)
  (modes (native))
  (flags (:standard -short-paths -warn-error -9))
  (preprocess (pps (ppx_jane)))
  (libraries
    (core async spawner parallel))))
