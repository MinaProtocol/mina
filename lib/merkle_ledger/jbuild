(jbuild_version 1)

(executable
 ((name merkle_ledger)
  (public_name merkle_ledger)
  (package merkle_ledger)
  (libraries (core ppx_inline_test.runtime-lib))
  (preprocess (pps (ppx_jane ppx_driver.runner (-inline-test-lib merkle_ledger))))))


