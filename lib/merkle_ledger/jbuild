(jbuild_version 1)

(library
 ((name merkle_ledger)
  (public_name merkle_ledger)
  (library_flags (-linkall))
  (libraries (core ppx_inline_test.runtime-lib))
  (preprocess (pps (ppx_jane ppx_driver.runner (-inline-test-lib nanobit_base))))
  (synopsis "Merkle Ledger")))


