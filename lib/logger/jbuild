(jbuild_version 1)

(library
 ((name logger)
  (public_name logger)
  (library_flags (-linkall))
  (inline_tests)
  (libraries (core async))
  (preprocess (pps (ppx_jane)))
  (synopsis "Logging library")))

