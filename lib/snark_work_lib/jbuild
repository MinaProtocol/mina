(jbuild_version 1)

(library
 ((name snark_work_lib)
  (public_name snark_work_lib)
  (flags (:standard -short-paths))
  (library_flags (-linkall))
  (inline_tests)
  (libraries
    ( core_kernel currency
      ))
  (preprocess (pps (ppx_jane)))
  (synopsis "Snark work types")))

