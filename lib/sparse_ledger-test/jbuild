(jbuild_version 1)

(executable
  ((name sparse_ledger_test)
   (package sparse_ledger)
   (public_name sparse_ledger_test)
   (libraries (sparse_ledger ppx_inline_test.runner.lib))
  ))

(alias
  ((name runtest)
   (deps (sparse_ledger_test.exe))
   (action (run ${<} inline-test-runner nanobit_base))))

