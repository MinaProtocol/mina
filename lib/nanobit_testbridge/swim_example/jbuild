(jbuild_version 1)

(executable
  ((name nanobit_testbridge_swim_example)
   (public_name nanobit_testbridge_swim_example)
   (package nanobit_testbridge)
   (libraries (core async async_extra testbridge nanobit_testbridge))
   (preprocess (pps (ppx_jane)))
   (flags (-short-paths -w -40 -g -warn-error -58))
   (modes (native))
  ))

