(jbuild_version 1)

(executable
  ((name nanobit_testbridge_recent_lca)
   (public_name nanobit_testbridge_recent_lca)
   (package nanobit_testbridge)
   (libraries (core async async_extra testbridge nanobit_testbridge))
   (preprocess (pps (ppx_jane)))
   (flags (-w -40 -g))
   (modes (native))
  ))

