(jbuild_version 1)

(executable
  ((name snarky_test)
   (package snarky)
   (public_name snarky_test)
   (libraries (snarky ppx_inline_test.runner.lib))
  ))

(alias
  ((name runtest)
   (deps (snarky_test.exe))
   (action (run ${<} inline-test-runner snarky))))

