(jbuild_version 1)

(library
 ((name cli_lib)
  (public_name cli_lib)
  (flags (:standard -short-paths -warn-error -58))
  (library_flags (-linkall))
  (inline_tests)
  (modules (cli_lib secret_box))
  (libraries
    ( core
      async_unix
      sodium
      coda_base ))
  (preprocess (pps (ppx_jane)))
  (synopsis "Some cli utilities")))

