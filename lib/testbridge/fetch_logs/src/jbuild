(jbuild_version 1)

(executable
  ((name fetch_logs)
   (public_name fetch_logs)
   (package testbridge)
   (libraries (core async async_extra testbridge))
   (preprocess (pps (ppx_jane)))
   (flags (-short-paths -w -40 -g -warn-error -58))
   (modes (native))
  ))

