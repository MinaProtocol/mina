(jbuild_version 1)

(executable
  ((name echo_client)
   (public_name echo_client)
   (package echo)
   (libraries (core async async_extra))
   (preprocess (pps (ppx_jane)))
   (flags (-w -40 -g))
   (modes (native))
  ))

