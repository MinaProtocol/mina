(jbuild_version 1)

(executable
  ((name ccc_host)
   (public_name ccc_host)
   (package testbridge)
   (libraries (core async async_extra testbridge))
   (preprocess (pps (ppx_jane)))
   (flags (-w -40 -g))
   (modes (native))
  ))
