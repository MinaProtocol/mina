(jbuild_version 1)

(executable
  ((name ccc_client)
   (public_name ccc_client)
   (package ccc)
   (libraries (core async async_extra))
   (preprocess (pps (ppx_jane)))
   (flags (-w -40 -g))
   (modes (native))
  ))

