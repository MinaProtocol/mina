(jbuild_version 1)

(executable
  ((name stdout_client)
   (public_name stdout_client)
   (package stdout)
   (libraries (core async async_extra))
   (preprocess (pps (ppx_jane)))
   (flags (-w -40 -g -warn-error -58))
   (modes (native))
  ))

