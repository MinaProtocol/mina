(jbuild_version 1)

(library
 ((name snark_pool)
  (public_name snark_pool)
  (inline_tests)
  (library_flags (-linkall))
  (libraries (core extlib protocols async linear_pipe))
  (preprocess (pps (ppx_jane ppx_deriving.eq)))
  (synopsis "Lib containing snark pool")))

