(jbuild_version 1)

(library
 ((name address)
  (public_name address)
  (flags (:standard -short-paths -warn-error -58))
  (library_flags (-linkall))
  (inline_tests)
  (libraries ( core bitstring ))
  (preprocess (pps (ppx_jane ppx_hash ppx_deriving.eq bitstring.ppx)))
  (synopsis "Union of intervals data structure")))
