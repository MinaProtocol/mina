(jbuild_version 1)

(executable
  ((name gen_values)
  (libraries
    ( async
      core
      crypto_params
      snarky
      ppxlib
    ))
   (preprocess (pps (ppx_jane ppxlib.metaquot ppxlib.runner)))
   (flags (-w -40 -g -warn-error -58))
   (modes (native))
  ))
