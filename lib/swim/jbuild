(jbuild_version 1)

(library
 ((name swimlib)
  (public_name swimlib)
  (library_flags (-linkall))
  (libraries (core linear_pipe async async_extra ppx_inline_test.runtime-lib))
  (preprocess (pps (ppx_jane ppx_driver.runner (-inline-test-lib swimlib))))
  (synopsis "SWIM - Scalable Weakly-consistent Infection-style Process Group Membership Protocol")))

