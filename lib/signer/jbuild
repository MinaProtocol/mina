(jbuild_version 1)

(library
 ((name signer)
  (public_name signer)
  (flags (:standard -short-paths))
  (library_flags (-linkall))
  (inline_tests)
  (libraries
    ( core
      async
      minibit
      coda
      nanobit_base
      protocols
      async_extra ))
  (preprocess (pps (ppx_jane)))
  (synopsis "Coda signer")))

