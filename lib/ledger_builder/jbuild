(jbuild_version 1)

(library
 ((name ledger_builder)
  (public_name ledger_builder)
  (library_flags (-linkall))
  (inline_tests)
  (libraries (core parallel_scan transaction_snark nanobit_base protocols coda linear_pipe logger async async_extra))
  (preprocess (pps (ppx_jane ppx_deriving.eq ppx_deriving.make)))
  (synopsis "Ledger builder updates the current ledger with new transactions")))

