(jbuild_version 1)

(library
 ((name hlist)
  (public_name hlist)
  (flags (:standard -short-paths -warn-error -58))
  (library_flags (-linkall))
  (inline_tests)
  (libraries ( core ))
  (preprocess (pps (ppx_jane)))
  (synopsis "Hlist")))

