(jbuild_version 1)

(library
 ((name sparse_ledger)
  (public_name sparse_ledger)
  (library_flags (-linkall))
  (libraries (core extlib ppx_inline_test.runtime-lib))
  (preprocess (pps (ppx_jane ppx_deriving.eq ppx_driver.runner (-inline-test-lib sparse_ledger))))
  (synopsis "sparse Ledger implementation")))

