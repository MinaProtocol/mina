(jbuild_version 1)

(executable
  ((name nanobit_base_test)
   (package nanobit_base)
   (public_name nanobit_base_test)
   (libraries (nanobit_base ppx_inline_test.runner.lib))
  ))

(alias
  ((name runtest)
   (deps (nanobit_base_test.exe))
   (action (run ${<} inline-test-runner nanobit_base))))

