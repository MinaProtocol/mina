(jbuild_version 1)

(executable
  ((name kademliatest)
   (package kademlia)
   (public_name kademliatest)
   (libraries (kademlia ppx_inline_test.runner.lib))
  ))

(alias
  ((name runtest)
   (deps (kademliatest.exe))
   (action (run ${<} inline-test-runner kademlia))))

