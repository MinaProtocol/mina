(jbuild_version 1)

(library
 ((name proposer)
  (public_name proposer)
  (flags (:standard -short-paths -warn-error -6))
  (library_flags (-linkall))
  (inline_tests)
  (libraries
    ( core
      async
      minibit
      coda
      nanobit_base
      protocols
      async_extra ))
  (preprocess (pps (ppx_jane)))
  (synopsis "Coda block proposer")))
