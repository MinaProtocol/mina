(jbuild_version 1)

(executables
 ((names (tutorial))
  (modes (native))
  (preprocess (pps (ppx_jane ppx_driver.runner)))
  (libraries (snarky core))))

;(install
;  ((section bin)
;    (files ((tutorial.exe as tutorial)))))
