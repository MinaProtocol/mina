(library
 ((name snarky)
  (public_name snarky)
  (flags (:standard -short-paths))
  (inline_tests)
  (libraries (core_kernel bitstring_lib interval_union bignum camlsnark_c))
  (c_library_flags (:standard -lstdc++ -lpthread))
  (flags (:standard -safe-string))
  (cxx_flags
    (
      (:standard "\\" -pedantic)
      (-I re2_c/libre2)
      ))
  (preprocess (pps (ppx_jane ppx_deriving.enum ppx_deriving.eq)))))

(jbuild_version 1)

