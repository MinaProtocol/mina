(jbuild_version 1)

(executable
  ((name swimtest)
   (package swimlib)
   (public_name swimtest)
   (libraries (swimlib ppx_inline_test.runner.lib))
  ))

(alias
  ((name runtest)
   (deps (swimtest.exe))
   (action (run ${<} inline-test-runner swimlib))))

