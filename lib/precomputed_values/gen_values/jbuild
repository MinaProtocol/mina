(jbuild_version 1)

(executable
  ((name gen_values)
  (libraries
    ( async
      core
      snarky
      snark_params
      nanobit_base
      blockchain_snark
      keys_lib
      snark_keys
      ppxlib
      ppx_util
    ))
   (preprocess (pps (ppx_jane ppxlib.metaquot ppxlib.runner)))
   (flags (-w -40 -g -warn-error -58))
   (modes (native))
  ))
