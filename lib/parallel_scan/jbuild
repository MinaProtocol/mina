(jbuild_version 1)

(library
 ((name parallel_scan)
  (public_name parallel_scan)
  (library_flags (-linkall))
  (libraries (linear_pipe core async async_extra ppx_inline_test.runtime-lib))
  (preprocess (pps (ppx_jane ppx_deriving.eq ppx_driver.runner (-inline-test-lib parallel_scan))))
  (synopsis "Parallel scan over an infinite stream (incremental map-reduce)")))

