(jbuild_version 1)

(executable
  ((name main)
   (libraries (core_kernel async core ))
   (preprocess (pps (ppx_jane)))
   (flags (-w -40 -g))
   (modes (native))
  ))

(install
 ((section bin)
  (files ((main.exe as main)))))
