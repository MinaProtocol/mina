FROM build-deps AS build-deps-bullseye

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update --quiet \
  && apt-get upgrade --quiet --yes \
  && apt-get install --no-install-recommends --quiet --yes \
    libssl-dev \
    libprocps-dev \
  && rm -rf /var/lib/apt/lists/*
